<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %> - WatchMan</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1><%= article.title %></h1>
    </header>
    <div class="content">
    <main>
        <section id="article-content">
            <p><strong>Category:</strong> <%= article.category %></p>
            <p><%= article.content %></p>
            <% if (article.image) { %>
                <img src="/uploads/<%= article.image %>" alt="Article Image" style="width:100%; height:auto;">
            <% } %>
            <% if (article.video) { %>
                <video controls style="width:100%; height:auto;">
                    <source src="/uploads/<%= article.video %>" type="video/mp4">
                </video>
            <% } %>
            <p><em>Posted on <%= new Date(article.createdAt).toLocaleString() %></em></p>
        </section>
    </main>
    </div>
    <footer>
        <p>Contact us: <a href="mailto:thewatchman@gmail.com">thewatchman@gmail.com</a></p>
    </footer>
</body>
</html>
